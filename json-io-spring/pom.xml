<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.cedarsoftware</groupId>
        <artifactId>json-io-parent</artifactId>
        <version>4.110.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>json-io-spring-boot-starter</artifactId>
    <packaging>jar</packaging>
    <name>json-io-spring-boot-starter</name>
    <description>Spring Boot starter for json-io - provides auto-configuration for JSON, JSON5, and TOON format support</description>

    <!--
      Spring Boot compatibility. The starter is compiled ONCE against a FLOOR (Spring Boot 3.4 / Spring
      6.2) and Spring is PROVIDED: the consuming app supplies Spring at ITS version at runtime, so the
      host's Spring Boot version wins and json-io forces NO Spring version onto consumers.

      The shipped auto-config uses only Spring FRAMEWORK-level APIs (WebMvcConfigurer, WebFluxConfigurer,
      ServerCodecConfigurer) and orders itself relative to Boot's auto-configs BY NAME (afterName), so it
      carries no Boot-version-specific type and runs on both Spring Boot 3.x AND 4.x from one artifact:
        mvn test                  -> floor, Spring Boot 3.4 (default)
        mvn test -Pspring-ceiling -> Spring Boot 3.5.x, full suite (forward-compat within Boot 3.x)
        mvn test -Pspring-boot4   -> Spring Boot 4.1 / Spring 7, RUNTIME check of the auto-config beans,
                                     converters, and codecs. The Boot-test-slice integration tests
                                     (@AutoConfigureMockMvc, TestRestTemplate) are excluded there because
                                     those Boot test slices moved packages in Boot 4; full HTTP
                                     integration coverage runs on Boot 3.x.
    -->
    <properties>
        <spring-boot.floor>3.4.13</spring-boot.floor>       <!-- oldest supported: Spring 6.2; compile target + default test runtime -->
        <spring-boot.ceiling>3.5.14</spring-boot.ceiling>    <!-- newest certified: latest Boot 3.5.x, still Spring 6.2 (exercised via -Pspring-ceiling) -->
        <spring-boot.boot4>4.1.0</spring-boot.boot4>         <!-- Spring Boot 4 / Spring 7 runtime check (via -Pspring-boot4; slice-based integration tests excluded) -->
        <spring-boot.bom>${spring-boot.floor}</spring-boot.bom>   <!-- active BOM version; default = floor -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.bom}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- json-io core (the only thing this starter contributes to a consumer's classpath) -->
        <dependency>
            <groupId>com.cedarsoftware</groupId>
            <artifactId>json-io</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring — PROVIDED: compiled against the floor, supplied by the host Spring Boot app at runtime.
             Versions come from the spring-boot-dependencies BOM above (nothing is forced on consumers). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Web (for HttpMessageConverter) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Web MVC (for WebMvcConfigurer) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring WebFlux (optional, for reactive support) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies (BOM-managed to the floor) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${version.maven-compiler-plugin}</version>
                <configuration>
                    <release>17</release>
                    <testRelease>17</testRelease>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>

            <!-- Required for Maven Central deployment -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Forward-compatibility test run: build + test against the CEILING (latest Spring Boot 3.5.x)
             instead of the floor. Usage: mvn test -Pspring-ceiling -->
        <profile>
            <id>spring-ceiling</id>
            <properties>
                <spring-boot.bom>${spring-boot.ceiling}</spring-boot.bom>
            </properties>
        </profile>

        <!-- Spring Boot 4 / Spring 7 RUNTIME check: build + run the Framework-level tests (auto-config
             beans, converters, codecs) against Spring Boot 4.1 to prove the starter actually RUNS on
             Boot 4 (not merely compiles). The seven Spring-Boot-test-slice integration tests
             (@AutoConfigureMockMvc, TestRestTemplate, auto-config class literals) are excluded — those
             slices moved in Boot 4 with no string-based fallback and would need a separate Boot-4 test
             module (see estimate). Usage: mvn clean test -Pspring-boot4 (JDK 17+). -->
        <profile>
            <id>spring-boot4</id>
            <properties>
                <spring-boot.bom>${spring-boot.boot4}</spring-boot.bom>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <testExcludes>
                                <testExclude>**/integration/*.java</testExclude>
                                <testExclude>**/JacksonCoexistenceModeTest.java</testExclude>
                            </testExcludes>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <excludes>
                                <exclude>**/integration/*.java</exclude>
                                <exclude>**/JacksonCoexistenceModeTest.java</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
