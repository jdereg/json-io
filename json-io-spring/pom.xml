<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.cedarsoftware</groupId>
        <artifactId>json-io-parent</artifactId>
        <version>4.106.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>json-io-spring-boot-starter</artifactId>
    <packaging>jar</packaging>
    <name>json-io-spring-boot-starter</name>
    <description>Spring Boot starter for json-io - provides auto-configuration for JSON, JSON5, and TOON format support</description>

    <!--
      Spring Boot version RANGE this single artifact supports: FLOOR Spring Boot 3.4 (Spring 6.2)
      → CEILING latest Spring Boot 3.5.x (Spring 6.2). The starter is compiled ONCE against the floor
      and Spring is PROVIDED: the consuming app supplies Spring at ITS version at runtime, so the host's
      Spring Boot version wins and json-io forces NO Spring version onto consumers. Floor-compiled
      bytecode links forward across the Boot 3.x line; `mvn test -Pspring-ceiling` runs the suite at the
      ceiling (${spring-boot.ceiling}), driven by ${spring-boot.bom} (default = floor).

      NOTE: the range stops at Boot 3.x on purpose. Spring Boot 4 relocated the web auto-config packages
      this starter imports (org.springframework.boot.autoconfigure.web.servlet / .web.reactive and
      org.springframework.boot.web.codec), so a single artifact cannot ALSO compile against Boot 4 /
      Spring 7 — supporting Boot 4 would require a separate Boot-4 source variant.
    -->
    <properties>
        <spring-boot.floor>3.4.13</spring-boot.floor>       <!-- oldest supported: Spring 6.2; compile target + default test runtime -->
        <spring-boot.ceiling>3.5.14</spring-boot.ceiling>    <!-- newest certified: latest Boot 3.5.x, still Spring 6.2 (exercised via -Pspring-ceiling) -->
        <spring-boot.bom>${spring-boot.floor}</spring-boot.bom>   <!-- active BOM version; default = floor -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.bom}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- json-io core (the only thing this starter contributes to a consumer's classpath) -->
        <dependency>
            <groupId>com.cedarsoftware</groupId>
            <artifactId>json-io</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spring — PROVIDED: compiled against the floor, supplied by the host Spring Boot app at runtime.
             Versions come from the spring-boot-dependencies BOM above (nothing is forced on consumers). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Web (for HttpMessageConverter) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Web MVC (for WebMvcConfigurer) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring WebFlux (optional, for reactive support) -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies (BOM-managed to the floor) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${version.maven-compiler-plugin}</version>
                <configuration>
                    <release>17</release>
                    <testRelease>17</testRelease>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <parameters>true</parameters>
                </configuration>
            </plugin>

            <!-- Required for Maven Central deployment -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Forward-compatibility test run: build + test against the CEILING (latest Spring Boot 3.5.x)
             instead of the floor. Usage: mvn test -Pspring-ceiling -->
        <profile>
            <id>spring-ceiling</id>
            <properties>
                <spring-boot.bom>${spring-boot.ceiling}</spring-boot.bom>
            </properties>
        </profile>
    </profiles>
</project>
